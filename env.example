# =============================================
# Ketchup Local Development Environment
# =============================================

# --- Database (used by backend) ---
DATABASE_URL=postgresql://postgres:postgres@db:5432/appdb

VLLM_MODEL=Qwen/Qwen3-4B-Instruct
PLANNER_FALLBACK_ENABLED=true
BACKEND_INTERNAL_API_KEY=dev-internal-key-change-me
# Requires Maps Platform server-side APIs enabled:
# - Places API (New)
# - Routes API
GOOGLE_MAPS_API_KEY=your-google-maps-server-key

# --- Auth.js / NextAuth (used by frontend) ---
# Generate with: openssl rand -base64 32
AUTH_SECRET=change-me-to-a-random-string-at-least-32-chars

# --- Google OAuth (used by frontend for sign-in) ---
# Create at: https://console.cloud.google.com/apis/credentials
# Type: OAuth 2.0 Client ID → Web application
# Authorized JavaScript origins: http://localhost:3001
# Authorized redirect URIs: http://localhost:3001/api/auth/callback/google
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here

# NEW: SMTP for invite emails
# For Gmail: enable 2FA, then create an App Password at
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ketchup.noreply@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_EMAIL=ketchup.noreply@gmail.com

# --- Backend URL (used by frontend proxy, inside Docker network) ---
BACKEND_URL=http://backend:8000

# --- Frontend URL (used by backend for CORS) ---
FRONTEND_URL=http://localhost:3001
